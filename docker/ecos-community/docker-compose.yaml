version: '3'
services:
  ui:
    container_name: ecos-ui-develop-extdev
    ports:
      - 80:80/tcp
    image: nexus.citeck.ru/ecos-ui:develop-extdev
    environment:
      - PROXY_TARGET=$ALFRESCO_HOST
