<webscript>
	<shortname>Global properties</shortname>
	<description>
		Show global properties.
		There are non mandatary parameters: 
			name - it is name of the requested property.
			jsonEscape - if it is not set or it is true, the output
				will be encoded in JSON format, in the other case 
				(jsonEscape=false) it will returns plain data.
	</description>
	<url>/citeck/global-properties</url>
	<format default="json">argument</format>
	<authentication>user</authentication>
	<transaction>required</transaction>
</webscript>
