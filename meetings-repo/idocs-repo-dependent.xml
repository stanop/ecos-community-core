<?xml version='1.0' encoding='UTF-8'?>
<project xmlns:artifact="antlib:org.apache.maven.artifact.ant" name="idocs-repo-dependent" default="resolve-dependencies">
    <import file="../repo-macros.xml"/>

    <property name="1st-override-repo.version" value="${idocs-core.version}"/>
    <property name="1st-override-repo.classifier" value="${idocs-core.classifier}"/>

    <property name="idocs-repo.version" value="${idocs-core.version}"/>
    <property name="idocs-repo.classifier" value="${idocs-core.classifier}"/>

    <path id="project.classpath">
        <fileset refid="1st-override-repo.jar"/>
        <fileset refid="idocs-repo.jar"/>
    </path>

    <target name="declare-repository">
        <artifact:remoteRepository id="citeck.repository" url="${citeck.artifacts.url}">
            <authentication username="${citeck.artifacts.user}" password="${citeck.artifacts.password}"/>
            <snapshots updatePolicy="${citeck.artifacts.updatePolicy}"/>
        </artifact:remoteRepository>
    </target>

    <target name="resolve-dependencies" depends="declare-repository">
        <resolve-dependency
                groupId="ru.citeck.idocs.core"
                artifactId="1st-override-repo"
                version="${1st-override-repo.version}"
                classifier="${1st-override-repo.classifier}"
                type="jar"
                repository="citeck.repository"/>
        <resolve-dependency
                groupId="ru.citeck.idocs.core"
                artifactId="idocs-repo"
                version="${idocs-repo.version}"
                classifier="${idocs-repo.classifier}"
                type="jar"
                repository="citeck.repository"/>
    </target>

    <target name="package-dependencies" depends="declare-repository">
        <copy-dependency todir="${dist.deps.dir}"
                groupId="ru.citeck.idocs.core"
                artifactId="1st-override-repo"
                version="${1st-override-repo.version}"
                classifier="${1st-override-repo.classifier}"
                type="amp"
                repository="citeck.repository"/>
        <copy-dependency todir="${dist.deps.dir}"
                groupId="ru.citeck.idocs.core"
                artifactId="idocs-repo"
                version="${idocs-repo.version}"
                classifier="${idocs-repo.classifier}"
                type="amp"
                repository="citeck.repository"/>
    </target>

</project>
